---
name: sky-req-prd-system
description: 将产品经理已转换的零散产品需求（非用户原话）整理为持续维护的项目方案站点：固定项目入口、版本历史、版本差异、需求文档、可交互原型及本页需求面板、测试用例、验收与上线方案——解决产品经理重复性的需求文档与原型产出工作。输入过于简洁或泛化时，可适当挖掘补充细颗粒度需求（标注 AI 推断与待确认）。用户提供产品需求并明确要求生成、更新产品方案或交互原型时使用；仅讨论需求、审查技能或普通代码开发时不自动生成方案。
metadata:
  version: "1.1.0"
---

# 项目方案与交互交付

围绕同一产品项目持续维护需求。开发从一个固定入口查看本版范围、历史版本、可操作的业务流程，以及原型元素对应的需求和验收口径。

Skill 版本与用户项目版本独立。此 Skill 为 1.1；项目可以使用自己的 v0.1、v2.0 等版本号。

## 工作入口

已有物料的局部修改优先走 references/maintenance.md 的轻量入口；不继续执行以下首次生成/范围归纳流程，不默认加载所有参考。只有新增功能规划或结构性变更才进入完整工作流。

先定位项目，再确定本次版本与范围。只在用户指定的工作区及目标项目操作；不得因为输入是“一个功能”就新建独立项目。

1. 用户给出项目路径时直接读取该目录的 `project.json`。否则运行 `python3 <skill>/scripts/project.py discover <用户工作区>` 搜索已有项目。
2. 同名候选不唯一、或用户说“已有项目”但找不到时，询问项目位置。没有依据时不能自建项目替代。
3. 新项目：确认产品形态（web/desktop/mobile/h5/miniapp/tv），创建一次项目目录。已有项目：区分当前版本修订与下一版本规划；普通改动不升版本。
4. 已有旧版 `content/manifest.json` 时走迁移流程，读取 references/workflow.md。迁移保留旧物料及备份。
5. 读取全局 `<skill>/config.md` 与项目 `config.md` 的实际配置项；当场指令 > 项目配置 > 全局配置 > 默认规则。不能以配置取消来源标记、历史保护或验证。
6. 读取 references/workflow.md（结构、命令、修改保护）与 references/nodes.md（按范围选择物料）。生成或修改原型时再读 references/prototype.md。

## 归纳与范围

生成或修订 PRD、测试及上线方案前，读取 references/delivery-quality.md，按功能类型深化规则、实例及未决问题；应用公司规范。结构检查不能代替业务完整性评审。

从需求中归纳问题、目标用户、角色、形态、多端、本版目标、范围与排除项。信息充分时直接整理，缺失时仅问影响项目结构与版本归属的问题。用户拒绝补充后停止追问，将假设标为 ai-inferred/pending。

归纳清单包含：
- 项目及版本归属，修订现有版本还是规划下一版；
- 本版新增/修改/移除项，关联的已有功能、页面及流程；
- 要生成的物料，以及可继承或不适用的物料；
- 数字冲突、来源冲突、端与权限边界、待决策事项，区分是否阻塞开发。

**物料按需裁剪**：用户明确指定交付物（如「只要 PRD」「只要原型」「只要流程图 / 测试用例」）时，只生成所指模块，其余不生成、不占菜单——manifest 仅登记本次范围，后续说「补上测试 / 上线」再增量添加。未明确指定时按 references/nodes.md 场景裁剪出具全套物料。裁剪物料不裁剪质量：所出内容仍需稳定 ID、来源标记与 build+validate；含原型时仍需最小 spec 作为规则来源并做浏览器验证。

首次方案或重大范围变更需展示清单供用户确认。用户已经明确批准的方案直接执行；小修订展示影响后继续。不能为了填满菜单重新编写整套产品背景或无关测试。

## 数据与版本原则

用户从已生成 HTML 提出自然语言修改（需求编号、控件、显示、布局或行为）时，先读 references/incremental.md；从原项目精确定位再局部修订，不新建目录、不重写整页或整份 PRD。找不到源目录或目标有歧义时先澄清，不凭空生成替代项。

用户要求自行二次编辑 PRD、原型属性或用自然语言修订既有需求时，读取 references/editing.md。使用本机编辑器或共享保存命令修改正式源数据；检查冲突、预览影响、保存并验证，不能把浏览器批注当作正式编辑。原型属性支持点选布局预览，但实际业务行为另行验证。

- 一个项目一个 `site/index.html`；左侧按版本展开本版物料，项目总览显示版本计划。
- 每版在 `versions/<版本>/content/` 保存完整、独立的物料。创建下一版复制上一版作为基线，只编辑本次变化；已冻结版本的文档、原型及资源保持快照。
- 当前版本修订不创建新版本。版本状态 planning → frozen → developing → released；冻结后新需求进入下一版，历史勘误也在后续版注明。
- 同一产品版本需要多轮交接时，用 workflow.md 的 handoff 创建不可变 r1/r2 快照，工作版继续 planning。交接归属及当前开发依据单独记录，旧交接深链接不重定向；普通保存不自动交接。
- `project.json` 管版本；各版 manifest 管模块与页面；`spec.json` 管页面级需求、交互规则与验收，稳定 ID 不随标题改变。
- 原型页面 ID 同时作为可见页面编号，在左侧菜单和原型工具栏显示；与 manifest/spec/body data-page-id 一致，不因改名、重排或新版本继承重新编号。按“项目 + 版本 + 页面编号”定位增量修改。
- PRD 的结构化需求页和原型需求面板从同一份 spec 编译，不手工维护第二份相同规则。
- 来源 origin/ai-inferred 与确认状态 confirmed/pending 分开。AI 补全正文标明来源；发布状态不等于规则已确认。
- 修改前运行 impact，读取受影响文件及已有基线，保留用户手改内容。不能先 checkpoint 再检查，不能静默覆盖无法解释的改动。
- 运行时使用经典外部脚本，兼容 file://；原型允许独立 JS。源 HTML 禁止内联 script/style/事件属性。拖拽产生的运行时几何样式允许。
- 仅冻结版本资源；项目壳可以升级，历史内容与资源不随壳升级重写。

## 生成与验证

生成或调整 PRD 结构、数据埋点、非功能需求、计划风险、附录时，先读取 references/standard-prd.md。采用九章标准 PRD，章节显式标注已提供/待补充/不适用；范围和页面需求复用唯一来源。附件通过本机编辑服务真实落盘，静态查阅页仅下载，冻结后补充不改原快照。

1. 使用项目命令初始化或创建版本（命令见 workflow.md），先更新 manifest 与 spec 的稳定 ID 和关联。
2. 按确认的物料范围生成概览、背景文档、交互原型、必要流程、测试与上线说明（用户指定子集时仅生成对应模块，见「归纳与范围」）。可继承未变内容，但含概览时必须明确本次差异和排除项。落地后的一切修改走增量入口（见 workflow.md 修改方式矩阵），禁止整体重生成物料。继承版本中被继承的「待补充」章节，须按本版业务补写或显式标注不适用，不得原样留白交付。
   新建文档的小节、埋点事件、测试用例、里程碑、风险使用 maintenance.md 的稳定物料锚点；表格行使用稳定 ID 列。保留旧 ID，不为局部修订重排或补写整个项目。站点提供“定位并交给 Agent”，派生视图修改其源数据。
   PRD 面向业务阅读：需求名称优先，分层展示说明、规则、实例、验收和关联控件；编号用于搜索/精确修改，不以 FR/INT 列表代替正文。完整 PRD Markdown 导出保留同样层次，JSON 仅作为结构化交换格式。
3. 原型覆盖本版有业务意义的控件和只读数据：显示/禁用条件、字段口径、输入校验、动作、反馈、跳转、异常与边界。新建或深改元素用 rule_ids 关联精确规则；只读业务数据标记 data-business-value 并声明 inspect 关联。本页需求提供元素选择与待确认筛选。以覆盖度验收，取消“每页 3～5 条”的数量限制。
4. 每个原型引用 annotation-store、共享 runtime 和编译后的 spec-data；支持演示/标注切换、细粒度编号、手动新增/编辑/删除/恢复标记、本页需求拖拽面板、场景切换和重置。新增标记必须采用“选择控件或位置 → 落点附近弹出优先级与说明表单 → 确认增加”；保存后点击标记只查看，编辑需从查看卡片显式进入。手动标记按项目/版本/页面保存在浏览器，可导出/导入；它们是评审批注，不直接改写 PRD 或冻结文件。表单及业务行为由页面外部 JS 实现，不能把装饰性按钮当成功能完成。
5. `python3 <项目>/build.py` 后运行 `python3 <skill>/scripts/validate.py <项目>`。错误必须修复。关联校验不能代替业务交互测试。
6. 浏览器实际操作关键正常/失败路径，验证页面跳转、提交、弹框、场景状态和面板拖拽。至少验证直接打开原型与固定入口 iframe 两种方式；截图保存至用户指定产物目录。
7. 运行 impact 检查本次影响并复核关联原型、验收和文案；全部完成后 checkpoint 记录新基线。仅在用户确认冻结/发布时执行相应状态命令。

## 交付

记录实际验收、上线指标或规划下一版跟进时读取 references/outcomes.md。原型验证与真实产品验收分开；事后记录进入项目级台账，不改冻结物料。规划已有版本时检查转入事项，只有经过评审的行动才进入正式需求。

需要让开发通过链接复现指定角色、业务状态及模拟数据时读取 references/replay.md；场景来自 spec 的合成预设，独立隔离，不分享真实输入。页面脚本必须实际执行对应权限和状态规则。

处理原型评审反馈或批注整改时读取 references/review-loop.md。批注默认查看，显式处理；采纳关联正式需求，完成关联修改记录和验证说明，不通过修改批注状态代替正式需求变更。

给开发交接或导出清单时读取 references/developer-delivery.md。使用“需求范围与交接”的需求清单表格、分区详情与筛选导出，注明基线、当前开发依据、缺失分工/依赖和待确认规则；不把确认状态当作技术就绪或测试通过。

**交付清单（每次生成交互原型或修改物料后必须逐项执行并汇报）**：
1. 确保服务运行：`python3 scripts/serve.py 端口 /项目目录`，用 `curl -s -o /dev/null -w '%{http_code}' http://localhost:端口/site/index.html` 确认返回 200；
2. 汇报**服务地址**（如 `http://localhost:端口/site/index.html`）与**本地文件路径**（项目目录绝对路径 + `/site/index.html`），缺一不可；
3. 给出可直接进入当前版本页面的入口（含路由哈希）；
4. 说明本次版本及状态、修改范围、验证结果、剩余决策项。

保持项目目录位置以维持链接；长期团队访问需要固定服务或托管地址，不能把会话临时服务描述成长期在线链接。

只在用户需要时启动局域网共享；不自动发布、不主动提交或推送 Git。

## 参考与验证示例

- references/workflow.md：目录、schema、项目命令、迁移、快照和增量维护。
- references/nodes.md：按工作场景裁剪物料、版本范围、跨端规则。
- references/prototype.md：结构化需求与交互 schema、运行时接入、原型验收。
- references/delivery-quality.md：需求深化检查维度、rule_details 结构、测试与上线口径。
- references/standard-prd.md：九章标准 PRD、章节适用性声明与附件落盘。
- references/maintenance.md：已生成项目的轻量维护入口与稳定物料锚点。
- references/incremental.md：从 HTML/编号/控件做自然语言增量修订的单字段入口。
- references/editing.md：本机 PRD 编辑器、原型属性编辑、保存冲突与撤销。
- references/replay.md：角色与业务状态复现链接（合成数据）。
- references/review-loop.md：原型批注处理闭环与正式采纳流程。
- references/outcomes.md：验收执行与指标观测台账、更正与下一版跟进。
- references/developer-delivery.md：需求范围与交接视图、筛选导出与联调依赖。
- `python3 <skill>/scripts/create_demo.py <空目录>`：生成含已发布 V1.0 和规划 V1.1 的订单示例，演示状态不代表真实产品决策。
- `tests/`：分层测试，运行方式与环境要求见 `TESTS.md`（逻辑层零依赖，浏览器层需 playwright）。
