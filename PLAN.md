# Skill 优化计划

范围：Skill v1.1 的规范、模板、运行时和检查工具；既有用户项目不自动升级。2026-09-08 建立。

状态：待实施 / 进行中 / 已验证。已写入规范不等于对应界面已经实现。

F1～F3 结果（2026-09-09）：50 项 Python、16 项批注/交接模型测试及原型浏览器回归通过；新增浏览器专项验证查看/编辑分离、落点高亮、编辑取消保护、重新定位、删除撤销、导出→本机保存→全新浏览器查看、草稿备份后载入已保存批注、可复制维护上下文，以及同入口追加新版本与旧深链接。轻量入口只向模型返回候选和选中物料，未量化 token 降幅；构建与部分安全检查仍遍历全项目。静态页不直接写磁盘；保存批注需本机编辑器或 Agent，非实时多人协作。旧版项目和冻结资源不自动升级。本轮未提交推送。

E1/E2 结果（2026-09-09）：45 项 Python 测试通过；标准 PRD/附件浏览器专项和原 PRD 浏览器回归通过，截图检查完成。九章引用唯一来源，缺章显式待补，完整导出不只含编号；本机上传后刷新、下载内容一致，同名附件不覆盖，冻结后补充不改快照，摘要与并发冲突校验通过。静态 HTML 仅下载，在线多人上传、病毒扫描、附件删除/替换不在本批范围。只修改 Skill，既有用户项目未自动升级，本轮未提交推送。

本次追加 D1/D2（2026-09-09）：40 项 Python 测试及 PRD 专项/原型回归浏览器验证通过，交接 Markdown 4 项模型测试通过。验证从 HTML 深链接定位版本、规则 ID 查询、精确规则/控件属性修改、无关条目和冻结快照不变、冲突拒绝；新 PRD 目录/搜索、技术信息折叠、Agent 定位上下文及完整正文导出通过。自然语言由 Agent 理解，脚本只定位和校验保存；布局/JS 局部修改仍须实际交互验证。仅修改 Skill，本轮尚未提交推送。

| ID | 工作项 | 验收结果 | 状态 |
|---|---|---|---|
| A1 | 按功能类型深化需求；公司规范配置 | 已接入生成工作流；规则指导已检查，真实项目效果待观察 | 规范已完成 |
| A2 | 规则级结构与质量检查 | 稳定规则 ID、实例、验收关联；报告具体缺口，不用分数冒充就绪 | 已验证 |
| A3 | 控件与只读数据的开发查看 | 点选/下拉选择元素，按 rule_ids 展示分类规则、实例及验收，待确认筛选；只读数据关联检查及旧数据降级提示 | 已验证 |
| A4 | 测试与上线口径深化 | 规范已接入；执行结果录入和自动追踪留至 C4 | 规范已完成 |
| B1 | 正式 PRD 二次编辑 | 本机表单编辑既有需求/规则/实例/验收与 Markdown；预览、落盘、撤销、冲突保留草稿 | 已验证 |
| B2 | 原型属性编辑 | 点选静态布局；编辑文案/选项/原生约束/默认值/目标/三档宽度；共享保存撤销，真实原型行为验证 | 已验证 |
| B3 | 自然语言变更与规则级差异 | Agent 与浏览器复用保存命令；规则 ID 前后差异、页面/控件/验收影响；业务脚本明确待复核 | 已验证 |
| B4 | 同产品版本的交接修订 | handoff 创建 r1/r2 独立冻结快照；草稿继续编辑，当前开发依据与历史入口明确 | 已验证 |
| C1 | 开发交接视图及任务包 | 基线与开发依据、变化/待确认/阻塞/角色筛选，规则/原型/验收/依赖，当前筛选 Markdown/JSON 导出 | 已验证 |
| C2 | 评审批注闭环 | 显式状态处理、说明/FR/修改记录/验证说明、历史与筛选，改正文重新待处理；导出及本地记录核对 | 已验证 |
| C3 | 角色与状态复现链接 | 预设合成场景、版本/页面链接、角色与初始数据还原、跨页隔离、重置及失效提示 | 已验证 |
| C4 | 验收结果与上线复盘 | 原型/产品结果分离、证据与基线、指标观测、更正留痕、下一版跟进；草稿导出与正式入库 | 已验证 |
| D1 | 已有 HTML 自然语言精确修订 | HTML/版本定位、稳定 ID 查询、单字段预览保存、冲突/冻结保护；布局行为局部修改规范 | 已验证 |
| D2 | PRD 阅读层次与业务导出 | 业务标题、需求目录/筛选、规则与验收分区、控件折叠、完整 Markdown 与交接导出分层 | 已验证 |
| D3 | 原型页稳定编号与菜单展示 | 沿用页面 ID，菜单及原型顶部显示；按页精确查询源 HTML/需求/控件，跨页错误目标拒绝 | 已验证 |
| D4 | 需求范围与交接：改名及分层表格 | 菜单/标题/导出改名；摘要、需求清单 table、单项分区详情；筛选导出一致，旧路由兼容 | 已验证 |
| E1 | 九章标准 PRD 与同源阅读 | 章节状态、范围/功能/验收同源、九章目录、完整正文导出、旧路由兼容 | 已验证 |
| E2 | 附录真实文件上传 | 本机编辑器上传/下载、唯一 ID 落盘、关联需求、摘要校验、冻结后补充标识、静态只读链接 | 已验证 |
| F1 | 轻量维护与全物料定位 | 独立维护路由、模块/页面/规则/文档/锚点/表格行/附件索引、精简查询、唯一原文补丁、跨 Agent 上下文 | 已验证 |
| F2 | Web 标注操作与共享保存 | 开发只读/产品编辑分离、高亮落点、显式编辑/移动、取消保护、删除撤销、导出后本机保存、旧草稿冲突拒绝 | 已验证 |
| F3 | 原项目追加版本防误建 | 规划草稿显式选基线、复用项目 ID/入口、冻结哈希保护、新菜单和历史深链接回归 | 已验证 |

顺序：A1/A2/A4 → A3 → B1/B3 → B2/B4 → C1～C4。编辑服务先实现本机保存；实时多人协作、自由画布编辑、外部任务系统写入后置。

验证：只用临时项目；保留旧 schema 与冻结快照；单元检查覆盖真实缺口，界面变更另做浏览器验证。以开发追问次数、遗漏返工、变更同步耗时和评审到开工耗时评价实际收益，未测量前不宣称提升比例。

首批结果：15 项 Python 测试通过；浏览器验证 iframe/独立页面的细粒度规则展示、业务操作与标注回归通过，无页面错误；Skill 校验与 diff 检查通过。audit_quality 已在临时示例上验证，正确报告旧需求无结构化规则及新增规则待确认。

A3 结果：17 项 Python 测试通过；浏览器验证精确规则隔离、待确认筛选、弹框内字段定位、只读数据与旧数据降级，以及既有交互回归；截图检查通过，无页面错误。只读数据的静态检查仅覆盖已声明元素，未声明的数据仍需业务审阅。下一工作项 B1/B3：正式编辑与变更影响。

B1/B3 结果：27 项 Python 测试通过，覆盖保存/撤销、历史与基线保护、写入失败回滚、外部变更冲突、正文路径限制、规则差异、会话授权及 CLI 预览。浏览器验证编辑→预览→保存→刷新、撤销、冲突保留草稿通过，无页面错误，截图已检查。新增 scripts/edit_project.py 和 references/editing.md。暂不支持自动合并、浏览器新增/删除结构条目、原型属性编辑、多人共享编辑；macOS/Linux 本机服务。修改源数据后不自动 checkpoint。下一工作项 B2：原型属性编辑；其后 B4 交接修订。

B2 结果：29 项 Python 测试通过；浏览器验证布局点选、文案、maxlength 40→60 后真实提交 50 字、选项/默认值/必填、目标切换、宽度、撤销及 PRD 编辑冲突保护；既有原型交互回归通过。原型属性由 spec 驱动，PRD 和需求面板共用配置；属性类型/目标校验，旧 runtime 拒绝静默使用新属性。布局预览不执行原型脚本；自定义脚本、旧规则文字与提示仍需按影响报告复核，关联规则改回 pending。自由拖拽布局和动态控件结构不在本批范围。下一工作项 B4：同版本交接修订。

B4 结果（2026-09-09）：31 项 Python 测试通过；浏览器验证 r1 深链接、历史原型弹框、r2 当前开发依据、两轮独立需求正文通过。交接编号不允许覆盖，阻塞规则拒绝交接，历史哈希持续验证；同产品版本下归组，工作版不因交接而冻结。先 staging 验证并检查并发指纹，多文件落盘中断按 workflow.md 恢复。下一工作项 C1：开发交接视图与任务包。

C1 结果（2026-09-09）：31 项 Python 测试及 4 项交接模型测试通过；浏览器验证筛选、子规则待确认、筛选 JSON 导出、原型链接通过，截图检查完成。任务包复用视图数据，保留移除需求旧链接和验收；按需求/交互/页面关联及文件变化保守复核。缺分工/依赖明确待补，确认状态不作为技术就绪判断；不自动创建外部工单。下一工作项 C2：评审批注闭环。

C2 结果（2026-09-09）：32 项 Python 测试、12 项标注状态测试及浏览器回归通过。支持采纳→完成、缺证据阻止完成、处理状态筛选、修改批注重新待处理、撤销、旧格式导入与新版导出。review_notes.py 可核对本地正式保存记录和 FR；完成状态与验证说明由评审人填写，不代表实际产品自动验收。批注仍为本地评审覆盖层，不自动改 PRD、不自动多人同步。下一工作项 C3：角色与状态复现链接。

C3 结果（2026-09-09）：34 项 Python 测试、16 项标注/交接模型测试及原型浏览器回归通过；专项浏览器验证固定入口/独立链接、只读角色实际禁用、空态/失败/无权限、跨页状态、重置及普通演示隔离、无效上下文拒绝、存储不可用提示。链接分享 spec 中的预设合成数据起点，不含临时输入或批注，不是任意操作录像；页面 JS 负责真正实现权限，正式交接应分享冻结修订。仅修改 Skill，临时项目用于验证，未升级用户历史资源。下一工作项 C4：验收结果与上线复盘。

C4 结果（2026-09-09）：38 项 Python 测试、16 项标注/交接模型测试及浏览器专项验证通过。项目级追加台账不改冻结快照；更正保留旧结果，基线变动提示复核，原型/产品分离并显示未登记验收项。指标记录口径、基线、目标、实际、窗口与证据；行动关联已存在的规划版本。浏览器 JSON 草稿可二次编辑/导出，record_outcome.py 校验入库后需构建；正式 PRD 编辑保留台账。没有执行就不标通过，未独立核验证据，不自动上线、不自动创建需求或同步多人。A～C 实施队列已完成（A1/A4 为规范完成）；下一阶段建议用一项真实需求做端到端试用，根据产品/开发反馈决定后续优化。
